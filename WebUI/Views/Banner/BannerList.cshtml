@model List<ResultBannerDto>
@{
    ViewData["Title"] = "BannerList";
    Layout = "_LayoutAdmin";
    int count = 0;
}

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Kategori Listesi</div>
                    </div>
                    <div class="card-body">
                        @if (Model.Any())
                        {
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th scope="col"></th>
                                        <th scope="col">Başlık</th>
                                        <th scope="col" colspan="2"></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td>@count</td>
                                            <td>@item.BannerTitle</td>
                                            <td><a class="btn btn-info" asp-controller="Banner" asp-action="BannerUpdate" asp-route-id="@item.BannerId"><i class="la la-pencil"></i></a></td>
                                            <td><a class="btn btn-danger" asp-controller="Banner" asp-action="BannerDelete" asp-route-id="@item.BannerId"><i class="la la-trash"></i></a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        } else
                        {
                            <div class="alert alert-warning" role="alert">Öge mevcut değil</div>
                        }
                        <hr>
                        <a class="btn btn-success" asp-controller="Banner" asp-action="BannerCreate">Yeni</a>
                    </div>
                </div>
            </div>
        </div>
    </div>